{
  "name": "helix-estimator-emails",
  "private": true,
  "scripts": {
    "start": "cd helix-scraper && node server.js",
    "install": "cd helix-scraper && npm install",
    "build": "echo 'No build step needed'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
